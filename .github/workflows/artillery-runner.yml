name: Artillery Load Test (Multi-Runner)

# Trigger workflow manually from GitHub UI
on:
  workflow_dispatch:
    inputs:
      # Type of test: load, stress, or cleanup
      test_type:
        description: "Choose test type (e.g., load/cleanup)"
        required: true
        type: choice
        options:
          - load
          - stress
          - cleanup
      # Environment to run test against
      environment:
        description: "Environment to run test on (dev/stage/prod)"
        required: true
        type: choice
        options:
          - test
          - prod
      # Number of parallel runners (1–4)
      runners_to_use:
        description: "Number of parallel runners to use (1–4)"
        required: true
        type: choice
        options:
          - '1'
          - '2'
          - '4'
      # Artillery scenario YAML file
      scenario_file:
        description: "YAML file for Artillery test (e.g., cloudrun-loadtest.yml)"
        required: true
        default: "cloudrun-loadtest.yml"
      # Days after which old reports should be cleaned
      cleanup_days:
        description: "Delete reports older than X days (cleanup mode)"
        required: false
        default: "7"

# Global environment variables for all jobs
env:
  ARTILLERY_VERSION: "2.0.26"      # Artillery CLI version to install
  SCRIPTS_DIR: "./scripts"         # Folder containing YAML & hooks.js
  DATA_DIR: "./data"               # CSV payload folder
  REPORT_DIR: "./artillery-results" # Where JSON + HTML results will be stored

# =========================================================
# 1️⃣ Job: Run Artillery on Multiple Runners in Parallel
# =========================================================
jobs:
  run-artillery:
    name: Run Artillery - Runner ${{ matrix.runner_index }}
    runs-on: ubuntu-latest

    # Define up to 4 runners; only use as many as user requested
    strategy:
      fail-fast: false
      matrix:
        runner_index: [1, 2, 3, 4]

    steps:

      # ---------------------------------------------------------------
      # Step 0a: Checkout repository to access scripts/ and data/
      # ---------------------------------------------------------------
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full repo history, ensures all files are available

      # ---------------------------------------------------------------
      # Step 0b: Skip extra runners gracefully (show green ✔️ instead of ❌)
      # ---------------------------------------------------------------
      - name: 🛑 Skip unused runner jobs
        if: ${{ matrix.runner_index > fromJson(github.event.inputs.runners_to_use) }}
        continue-on-error: true
        run: |
          echo "Skipping runner ${{ matrix.runner_index }} (limit = ${{ github.event.inputs.runners_to_use }})"
          exit 0

      # ---------------------------------------------------------------
      # Step 1: Print runner system info (OS, CPU, memory, public IP)
      # ---------------------------------------------------------------
      - name: ℹ️ Print Runner Provisioning Details
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        shell: bash
        run: |
          echo "================== RUNNER DETAILS =================="
          echo "Runner index: ${{ matrix.runner_index }} / ${{ github.event.inputs.runners_to_use }}"
          echo "OS: ${{ runner.os }} (${{ runner.arch }})"
          echo "Kernel: $(uname -sro)"
          echo "CPU cores: $(nproc)"
          MEM_KB=$(grep MemTotal /proc/meminfo | awk '{print $2}')
          MEM_GB=$(printf "%.2f" "$(echo "$MEM_KB / 1024 / 1024" | bc -l)")
          echo "Memory: ${MEM_GB} GB"
          echo "Public IP: $(curl -s api.ipify.org)"
          echo "===================================================="

      # ---------------------------------------------------------------
      # Step 2: Setup Node.js (required to run Artillery)
      # ---------------------------------------------------------------
      - name: 🧩 Setup Node.js
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # ---------------------------------------------------------------
      # Step 3: Install Artillery CLI and metrics plugin
      # ---------------------------------------------------------------
      - name: ⚙️ Install Artillery + Plugin
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        run: |
          npm install -g artillery@${{ env.ARTILLERY_VERSION }}
          npm install --save-dev artillery-plugin-metrics-by-endpoint
          artillery --version

      # ---------------------------------------------------------------
      # Step 3b: Debug workspace and scripts folder
      # ---------------------------------------------------------------
      - name: 🧪 Debug List workspace & scripts folder
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        shell: bash
        run: |
          echo "Working directory: $(pwd)"
          echo "All files in workspace:"
          ls -R .
          echo "Contents of ./scripts folder:"
          ls -R ./scripts || echo "scripts folder not found!"

      # ---------------------------------------------------------------
      # Step 4: Run Artillery Test (generates JSON + HTML results)
      # Unique filenames per runner using timestamp + random suffix
      # ---------------------------------------------------------------
      - name: 🚀 Run Artillery Test
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        id: run_test
        shell: bash
        run: |
          mkdir -p artillery-results
          TEST_NAME="test-${{ github.event.inputs.test_type }}-${{ github.event.inputs.environment }}"
          TIMESTAMP=$(date +%d%m%y)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          RESULT_JSON="${TEST_NAME}-${TIMESTAMP}-runner-${{ matrix.runner_index }}-${RANDOM_SUFFIX}.json"
          RESULT_HTML="${TEST_NAME}-${TIMESTAMP}-runner-${{ matrix.runner_index }}-${RANDOM_SUFFIX}-full.html"

          echo "🧪 Running test: ${RESULT_JSON}"
          ARTILLERY_TARGET="${{ vars.BASE_URL }}" \
          API_KEY="${{ secrets.API_KEY }}" \
          AUTH_HEADER="${{ secrets.AUTH_HEADER }}" \
          npx artillery run "${{ env.SCRIPTS_DIR }}/${{ github.event.inputs.scenario_file }}" \
          --output "artillery-results/${RESULT_JSON}" --quiet
          npx artillery report "artillery-results/${RESULT_JSON}" --output "artillery-results/$REPORT_HTML"
          
          echo "result_json=${RESULT_JSON}" >> $GITHUB_OUTPUT
          echo "result_html=${RESULT_HTML}" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------------
      # Step 5: Package JSON + HTML + scenario YAML into ZIP per runner
      # ---------------------------------------------------------------
      - name: 📦 Package Artillery Results
        if: ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        run: |
          TEST_NAME="test-${{ github.event.inputs.test_type }}-${{ github.event.inputs.environment }}"
          ZIP_NAME="${TEST_NAME}-${{ matrix.runner_index }}.zip"
          mkdir -p upload
          zip -r "upload/${ZIP_NAME}" artillery-results/*${{ matrix.runner_index }}*.json artillery-results/*${{ matrix.runner_index }}*.html ${{ github.event.inputs.scenario_file }}
          echo "Packaged ZIP: upload/${ZIP_NAME}"

      # ---------------------------------------------------------------
      # Step 6: Upload ZIP artifact per runner
      # Always attempt to upload even if job is cancelled
      # ---------------------------------------------------------------
      - name: 📤 Upload Artillery Results (Always Run)
        if: always() && ${{ matrix.runner_index <= fromJson(github.event.inputs.runners_to_use) }}
        uses: actions/upload-artifact@v4
        with:
          name: artillery-results-runner-${{ matrix.runner_index }}
          path: |
            upload/*.zip
            artillery-results/*.json
            artillery-results/logs/error_log.txt
        continue-on-error: true

# =========================================================
# 2️⃣ Job: Aggregate All Runner Reports
# =========================================================
  aggregate-reports:
    if: ${{ github.event.inputs.test_type != 'cleanup' }}
    needs: [run-artillery]
    runs-on: ubuntu-latest

    steps:
      # ---------------------------------------------------------------
      # Step 1: Download all runner artifacts
      # ---------------------------------------------------------------
      - name: 📥 Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artillery-results

      # ---------------------------------------------------------------
      # Step 2: Extract ZIPs into separate folders
      # Prevent overwriting files from different runners
      # ---------------------------------------------------------------
      - name: 🧩 Extract ZIPs per runner
        id: extract
        shell: bash
        run: |
          mkdir -p combined-results
          for zipfile in artillery-results/**/*.zip; do
            foldername=$(basename "$zipfile" .zip)
            mkdir -p combined-results/"$foldername"
            unzip -o "$zipfile" -d combined-results/"$foldername" > /dev/null
          done
          echo "✅ Extracted all runner ZIPs into unique subfolders."

      # ---------------------------------------------------------------
      # Step 3: Collect all JSON result filenames for Python script
      # Outputs a comma-separated string
      # ---------------------------------------------------------------
      - name: 📝 Collect JSON filenames for Python
        id: collect_jsons
        shell: bash
        run: |
          JSON_FILES=""
          for folder in combined-results/*; do
            json_file=$(ls "$folder"/*.json | head -n 1)
            if [ -n "$json_file" ]; then
              JSON_FILES="$JSON_FILES,$(basename "$json_file")"
            fi
          done
          JSON_FILES="${JSON_FILES#,}"
          echo "json_files=$JSON_FILES" >> $GITHUB_OUTPUT
          echo "✅ JSON files to pass: $JSON_FILES"

      # ---------------------------------------------------------------
      # Step 4: Setup Python for dashboard generation
      # ---------------------------------------------------------------
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ---------------------------------------------------------------
      # Step 5: Install Python dependencies
      # ---------------------------------------------------------------
      - name: 📦 Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas pyyaml plotly numpy

      # ---------------------------------------------------------------
      # Step 6: Run Python script to generate merged HTML dashboard
      # ---------------------------------------------------------------
      - name: 🧮 Generate Combined Dashboard
        shell: bash
        run: |
          cp combined-results/*/*.yml config.yml
          python ../generate_artillery_dashboard.py \
            --json "${{ steps.collect_jsons.outputs.json_files }}" \
            --yaml config.yml \
            --output "../merged-dashboard.html"
          echo "✅ Merged dashboard generated: merged-dashboard.html"

      # ---------------------------------------------------------------
      # Step 7: Upload the merged HTML dashboard as artifact
      # ---------------------------------------------------------------
      - name: 📤 Upload Combined Dashboard
        uses: actions/upload-artifact@v4
        with:
          name: merged-artillery-dashboard
          path: merged-dashboard.html
